{
    "ConnectionStrings": {
        "DefaultConnection": "server=localhost; userid=postgres; password=root; port=5432; database=TodoAPI;",
        "Kestel": {
            "EndPoints": {
                "http1": { "url": "http://localhost:5000" }
            }
        }
    },
    "Logging": {
        "LogLevel": {
            "Default": "Warning"
        }
    },
    "AllowedHosts": "*"
}
